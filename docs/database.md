# Database

## Overview

Spendly uses Drift ORM on top of SQLite. Every read and write goes through Drift — there is no raw SQL in feature code. The database lives on-device and is the single source of truth for all app data.

---

## Why Drift

- Type-safe queries — compile errors instead of runtime crashes
- Stream-based watching — changes in the DB automatically push updates to the UI via Riverpod
- Code-generated DAOs — no boilerplate mapping between rows and Dart objects
- SQLite — fast, zero-configuration, works fully offline

---

## Schema

The database has 14 tables. All monetary amounts are stored in paise (1 ₹ = 100 paise) to avoid floating-point precision errors. All mutable entities support soft deletes via an `isDeleted` flag.

---

### transactions

Stores every income and expense event.

| Column | Type | Notes |
|---|---|---|
| id | TEXT (UUID) | Primary key |
| type | TEXT | `income` or `expense` |
| amount | REAL | Display amount (₹) |
| amountPaise | INTEGER | Exact amount in paise |
| categoryId | TEXT | FK → categories.id |
| paymentMode | TEXT | `cash`, `card`, `upi` |
| cardType | TEXT | `credit` or `debit` (optional) |
| date | DATETIME | Transaction date |
| note | TEXT | Optional description |
| recurringRuleId | TEXT | FK → recurringRules.id (null if not recurring) |
| isRecurringInstance | BOOLEAN | True if auto-generated by a recurring rule |
| isDeleted | BOOLEAN | Soft delete flag |

---

### categories

User-defined and default categories for organising transactions.

| Column | Type | Notes |
|---|---|---|
| id | TEXT (UUID) | Primary key |
| name | TEXT | Display name |
| icon | TEXT | Icon identifier |
| color | INTEGER | Color value |
| type | TEXT | `income` or `expense` |
| isDeleted | BOOLEAN | Soft delete flag |

Default categories are seeded from `core/database/default_categories.dart` on first launch.

---

### recurringRules

Defines the schedule for automatically generated transactions.

| Column | Type | Notes |
|---|---|---|
| id | TEXT (UUID) | Primary key |
| title | TEXT | Label shown in UI |
| type | TEXT | `income` or `expense` |
| amount | REAL | Amount per occurrence |
| amountPaise | INTEGER | Exact amount in paise |
| frequency | TEXT | `daily`, `weekly`, `monthly`, `yearly` |
| categoryId | TEXT | FK → categories.id |
| startDate | DATETIME | When the rule begins |
| nextDueDate | DATETIME | Next date to generate an instance |
| isActive | BOOLEAN | Pause/resume without deleting |
| isDeleted | BOOLEAN | Soft delete flag |

---

### settings

Single-row table. Row with `id = 1` is always present.

| Column | Type | Notes |
|---|---|---|
| id | INTEGER | Always 1 |
| monthlyBudget | REAL | Total monthly spending limit |
| currency | TEXT | Currency symbol (e.g. `₹`) |
| dailyReminderEnabled | BOOLEAN | Whether the daily notification is active |
| privacyLockEnabled | BOOLEAN | Whether biometric lock is on |
| showAmountsEnabled | BOOLEAN | Whether amounts are visible |
| lastBudgetAlertAt | DATETIME | Last time a budget alert was shown |

---

### userProfiles

Single-row table. Row with `id = 1` holds the current user.

| Column | Type | Notes |
|---|---|---|
| id | INTEGER | Always 1 |
| name | TEXT | Display name |
| email | TEXT | Optional |
| phone | TEXT | Optional |
| onboardingCompleted | BOOLEAN | Controls first-launch flow |

---

### lendPeople

People you have lent money to or borrowed from.

| Column | Type | Notes |
|---|---|---|
| id | TEXT (UUID) | Primary key |
| name | TEXT | Person's name |
| isDeleted | BOOLEAN | Soft delete flag |

---

### lendEntries

Individual lend or borrow transactions per person.

| Column | Type | Notes |
|---|---|---|
| id | TEXT (UUID) | Primary key |
| personId | TEXT | FK → lendPeople.id |
| type | TEXT | `lent` or `borrowed` |
| amount | REAL | Amount |
| isSettled | BOOLEAN | Fully settled flag |
| settledAmount | REAL | Amount settled so far |
| date | DATETIME | Entry date |
| note | TEXT | Optional note |
| isDeleted | BOOLEAN | Soft delete flag |

---

### lendSettlementEvents

Records each partial or full settlement payment.

| Column | Type | Notes |
|---|---|---|
| id | TEXT (UUID) | Primary key |
| entryId | TEXT | FK → lendEntries.id |
| personId | TEXT | FK → lendPeople.id |
| amount | REAL | Amount settled in this event |
| date | DATETIME | Settlement date |

---

### monthlyReflections

One free-text note per calendar month.

| Column | Type | Notes |
|---|---|---|
| monthKey | TEXT | Primary key, format `YYYY-MM` |
| note | TEXT | User's reflection text |
| updatedAt | DATETIME | Last edit timestamp |

---

### categoryBudgets

Monthly budget allocation per category.

| Column | Type | Notes |
|---|---|---|
| monthKey | TEXT | Composite PK with categoryId, format `YYYY-MM` |
| categoryId | TEXT | Composite PK, FK → categories.id |
| budgetAmount | REAL | Allocated budget |
| budgetAmountPaise | INTEGER | Exact amount in paise |

---

### activityEvents

Log of significant user actions throughout the app.

| Column | Type | Notes |
|---|---|---|
| id | TEXT (UUID) | Primary key |
| kind | TEXT | Event type (e.g. `transaction_added`, `sync_completed`) |
| title | TEXT | Short label |
| description | TEXT | Detail text |
| occurredAt | DATETIME | Timestamp |

---

### appUsageDays

Daily screen time accumulator.

| Column | Type | Notes |
|---|---|---|
| dateKey | TEXT | Primary key, format `YYYY-MM-DD` |
| totalSeconds | INTEGER | Total app usage in seconds |
| updatedAt | DATETIME | Last update timestamp |

---

### goalFunds

Financial goals and the emergency fund.

| Column | Type | Notes |
|---|---|---|
| id | TEXT (UUID) | Primary key |
| title | TEXT | Goal name |
| targetAmount | REAL | Amount to reach |
| savedAmount | REAL | Amount saved so far |
| monthlyContribution | REAL | Optional monthly savings target |
| isEmergency | BOOLEAN | True for the emergency fund (only one) |
| isDeleted | BOOLEAN | Soft delete flag |

---

### goalContributions

Individual contributions made towards a goal.

| Column | Type | Notes |
|---|---|---|
| id | TEXT (UUID) | Primary key |
| goalId | TEXT | FK → goalFunds.id |
| amount | REAL | Contribution amount |
| note | TEXT | Optional note |
| date | DATETIME | Contribution date |
| isDeleted | BOOLEAN | Soft delete flag |

---

## Data Flow

```text
User action (UI)
      ↓
Riverpod Provider calls Repository method
      ↓
Repository calls Drift DAO (insert / update / select)
      ↓
Drift executes SQL on SQLite file on device
      ↓
Drift stream emits updated rows
      ↓
StreamProvider pushes new value
      ↓
Widgets rebuild automatically
```

---

## Offline-First Guarantee

All reads and writes hit the local SQLite file. There is no network call in the data path. Cloud sync (Google Drive) is a separate background process that copies a JSON snapshot; it does not replace the local database as the source of truth.

---

## Future Enhancements

- Encrypted database file at rest
- Import from CSV / bank statements
- Full backup metadata table
- Multi-account support